<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TWITTER</title>
    <link rel="stylesheet" href="tweet.css">
</head>
<body>
  <div id="akki">
    <h1>Tweets</h1>
    <button><a href="http://localhost:3000/tweets/new">Post New Tweet</a></button>
  </div>


   <% for(post of posts){ %>
      <div id="tweetinfo">
        <p>USERNAME = @<%= post.username %></p>
        <p>TWEET =<%= post.tweet %></p>
        <button><a href="http://localhost:3000/tweets/<%= post.id %>">SEE TWEET DETAIL</a></button>
        <button><a href="http://localhost:3000/tweets/<%= post.id %>/edit">EDIT</a></button>
        <br><br>
        <form method="post"  action="http://localhost:3000/tweets/<%= post.id %>?_method=DELETE">
          <button id="deletebtn">DELETE TWEET</button>
        </form>
       
      </div>  
   <%  } %>

</body>
</html>